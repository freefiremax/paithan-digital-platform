/**
 * Authentication configuration placeholder (NextAuth / Auth.js)
 * Note: Under rules.md §9, auth/role logic is flagged for confirmation before finalizing
 */

export const authConfig = {
  providers: [],
  pages: {
    signIn: "/admin/login",
  },
  callbacks: {},
};
